# config/packages/knp_paginator.yaml
knp_paginator:
    page_range: 5
    default_options:
        page_name: page
        sort_field_name: sort
        sort_direction_name: direction
        distinct: true
        default_limit: 10
    template:
        sortable: '@KnpPaginator/Pagination/bootstrap_v5_bi_sortable_link.html.twig'
        pagination: 'pagination/bootstrap_v5_pagination_fr.html.twig'  # ← Ton template français
