<form method="post" action="{{ path('app_rapport_medical_controller_php_delete', {'id': rapport_medical.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rapport_medical.id) }}">
    <button class="btn">Delete</button>
</form>
