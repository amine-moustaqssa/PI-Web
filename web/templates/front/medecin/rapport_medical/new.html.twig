{% extends 'base.html.twig' %}

{% block title %}Nouveau rapport médical{% endblock %}

{% block body %}
<h1>Créer un nouveau rapport médical</h1>

{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary mt-2">Enregistrer</button>
{{ form_end(form) }}

<a href="{{ path('rapport_index') }}" class="btn btn-secondary mt-2">Retour à la liste</a>
{% endblock %}
