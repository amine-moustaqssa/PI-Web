{% extends 'admin/base_admin.html.twig' %}

{% block title %}Nouvelle consultation{% endblock %}

{% block body %}
<div class="d-sm-flex align-items-center justify-content-between mb-4">
    <h1 class="h3 mb-0 text-gray-800">
        <i class="fas fa-stethoscope text-primary mr-2"></i> Nouvelle Consultation
    </h1>
    <a href="{{ path('admin_consultation_index') }}" class="btn btn-secondary btn-sm">
        <i class="fas fa-arrow-left mr-1"></i> Retour à la liste
    </a>
</div>

<div class="card shadow mb-4">
    <div class="card-header py-3">
        <h6 class="m-0 font-weight-bold text-primary">
            <i class="fas fa-plus-circle mr-1"></i> Informations de la consultation
        </h6>
    </div>
    <div class="card-body">
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-primary mt-3">
                <i class="fas fa-save mr-1"></i> Créer
            </button>
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}
