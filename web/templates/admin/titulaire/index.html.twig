{% extends 'admin/base_admin.html.twig' %}

{% block title %}Gestion des Clients{% endblock %}

{% block body %}

{# Page Heading #}
<div class="d-sm-flex align-items-center justify-content-between mb-4">
    <h1 class="h3 mb-0 text-gray-800">Gestion des Clients</h1>
    <a href="{{ path('admin_titulaire_new') }}" class="d-none d-sm-inline-block btn btn-sm btn-primary shadow-sm">
        <i class="fas fa-user-plus fa-sm text-white-50 mr-1"></i> Ajouter un client
    </a>
</div>

{# Stats Cards #}
<div class="row mb-4">
    <div class="col-xl-3 col-md-6 mb-4">
        <div class="card border-left-primary shadow h-100 py-2">
            <div class="card-body d-flex justify-content-between align-items-center">
                <div>
                    <div class="text-xs font-weight-bold text-primary text-uppercase mb-1">Total Clients</div>
                    <div class="h5 mb-0 font-weight-bold text-gray-800">{{ titulaires|length }}</div>
                </div>
                <i class="fas fa-user-friends fa-2x text-gray-300"></i>
            </div>
        </div>
    </div>

    <div class="col-xl-3 col-md-6 mb-4">
        <div class="card border-left-success shadow h-100 py-2">
            <div class="card-body d-flex justify-content-between align-items-center">
                <div>
                    <div class="text-xs font-weight-bold text-success text-uppercase mb-1">Comptes vérifiés</div>
                    <div class="h5 mb-0 font-weight-bold text-gray-800">
                        {{ titulaires|filter(t => t.isVerified)|length }}
                    </div>
                </div>
                <i class="fas fa-check-circle fa-2x text-gray-300"></i>
            </div>
        </div>
    </div>

    <div class="col-xl-3 col-md-6 mb-4">
        <div class="card border-left-info shadow h-100 py-2">
            <div class="card-body d-flex justify-content-between align-items-center">
                <div>
                    <div class="text-xs font-weight-bold text-info text-uppercase mb-1">Avec profils médicaux</div>
                    <div class="h5 mb-0 font-weight-bold text-gray-800">
                        {{ titulaires|filter(t => t.profilsMedicaux|length > 0)|length }}
                    </div>
                </div>
                <i class="fas fa-notes-medical fa-2x text-gray-300"></i>
            </div>
        </div>
    </div>
</div>

{# Data Table Card #}
<div class="card shadow mb-4">
    <div class="card-header py-3 d-flex justify-content-between align-items-center">
        <h6 class="m-0 font-weight-bold text-primary">Liste des clients</h6>
        <form method="get" class="d-flex" style="max-width: 350px;">
            <input type="text" name="q" class="form-control form-control-sm mr-2"
                   placeholder="Rechercher..." value="{{ search_query|default('') }}">
            <button type="submit" class="btn btn-sm btn-outline-primary">
                <i class="fas fa-search"></i>
            </button>
        </form>
    </div>

    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-bordered table-hover align-middle" id="titulaireTable" width="100%" cellspacing="0">
                <thead class="bg-light">
                    <tr>
                        <th style="width: 5%;">#</th>
                        <th>Client</th>
                        <th>E-mail</th>
                        <th class="text-center">Vérifié</th>
                        <th class="text-center">Profils médicaux</th>
                        <th>Adresse</th>
                        <th class="text-center" style="width: 15%;">Actions</th>
                    </tr>
                </thead>
                <tbody>
                {% for titulaire in titulaires %}
                    <tr>
                        <td class="text-muted small">{{ titulaire.id }}</td>
                        <td>
                            <div class="d-flex align-items-center">
                                <div class="rounded-circle bg-warning text-white d-flex align-items-center justify-content-center mr-3"
                                     style="width: 38px; height: 38px; font-size: 0.85rem; flex-shrink: 0;">
                                    {{ titulaire.prenom|first|upper }}{{ titulaire.nom|first|upper }}
                                </div>
                                <div>
                                    <div class="font-weight-bold text-gray-800">{{ titulaire.prenom }} {{ titulaire.nom }}</div>
                                    {% if titulaire.codePostal %}
                                        <div class="small text-muted">CP : {{ titulaire.codePostal }}</div>
                                    {% endif %}
                                </div>
                            </div>
                        </td>
                        <td><span class="small">{{ titulaire.email }}</span></td>
                        <td class="text-center">
                            {% if titulaire.isVerified %}
                                <span class="badge badge-success px-2 py-1"><i class="fas fa-check mr-1"></i>Oui</span>
                            {% else %}
                                <span class="badge badge-secondary px-2 py-1"><i class="fas fa-times mr-1"></i>Non</span>
                            {% endif %}
                        </td>
                        <td class="text-center">
                            <span class="badge badge-light border px-2 py-1">{{ titulaire.profilsMedicaux|length }}</span>
                        </td>
                        <td>
                            {% if titulaire.adresse %}
                                <span class="small">{{ titulaire.adresse }}</span>
                            {% else %}
                                <span class="text-muted small">—</span>
                            {% endif %}
                        </td>
                        <td class="text-center">
                            <a href="{{ path('admin_titulaire_show', {id: titulaire.id}) }}"
                               class="btn btn-info btn-circle btn-sm" title="Détails">
                                <i class="fas fa-eye"></i>
                            </a>
                            <a href="{{ path('admin_titulaire_edit', {id: titulaire.id}) }}"
                               class="btn btn-warning btn-circle btn-sm" title="Modifier">
                                <i class="fas fa-pen"></i>
                            </a>
                            <button class="btn btn-danger btn-circle btn-sm btn-delete-titulaire"
                                    data-id="{{ titulaire.id }}"
                                    data-name="{{ titulaire.prenom }} {{ titulaire.nom }}"
                                    title="Supprimer">
                                <i class="fas fa-trash"></i>
                            </button>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="7" class="text-center py-5">
                            <i class="fas fa-user-friends fa-3x text-gray-300 mb-3 d-block"></i>
                            <h6 class="text-gray-500">Aucun client trouvé</h6>
                            <a href="{{ path('admin_titulaire_new') }}" class="btn btn-primary btn-sm mt-2">
                                <i class="fas fa-plus mr-1"></i> Ajouter un client
                            </a>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>

{# Delete Confirmation Modal #}
<div class="modal fade" id="deleteModal" tabindex="-1" role="dialog" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title font-weight-bold text-gray-800">Confirmer la suppression</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Fermer">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                Êtes-vous sûr de vouloir supprimer <strong id="deleteTitulaireName"></strong> ?
                <br><small class="text-danger">Cette action est irréversible. Tous les profils médicaux associés seront également supprimés.</small>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Annuler</button>
                <form id="deleteForm" method="post">
                    <input type="hidden" name="_token" id="deleteToken">
                    <button type="submit" class="btn btn-danger">
                        <i class="fas fa-trash mr-1"></i> Supprimer
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>

{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        $(document).ready(function() {
            $('#titulaireTable').DataTable({
                language: {
                    url: "//cdn.datatables.net/plug-ins/1.13.6/i18n/fr-FR.json"
                },
                pageLength: 10,
                searching: false,
                order: [[1, 'asc']]
            });

            $('.btn-delete-titulaire').on('click', function() {
                var id = $(this).data('id');
                var name = $(this).data('name');
                $('#deleteTitulaireName').text(name);
                $('#deleteForm').attr('action', '/admin/titulaire/' + id + '/supprimer');
                $('#deleteToken').val('delete' + id);
                $('#deleteModal').modal('show');
            });
        });
    </script>
{% endblock %}
